let users=['angel,adrian,capella,ruiz'];
let blackListed=['capella,ruiz'];

function nombres(users,blackListed){
    let resultado=resultado.every(users => users>blackListed);
    return resultado;
    
}
console.log(nombres(resultado));